# Source-To-Intent Parser Corpus

These source buffers define the first research corpus for a future
Source-to-Intent parser. They are fixtures only. Loading them must not import
frontend modules, evaluate decorators, execute `@triton.jit`, produce
`source_intent.v0`, or construct compiler artifacts.
