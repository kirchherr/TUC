# Source Intent Intake Corpus

These JSON files are seed inputs for Source Intent Intake property tests.

They are plain-data fixtures only. The production intake API does not load
files, parse source text, consume preflight reports, or produce compiler
artifacts directly.
